% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/freestile_h3.R
\name{freestile_h3}
\alias{freestile_h3}
\title{Create vector tiles with dynamic H3 hexagonal binning}
\usage{
freestile_h3(
  input,
  output,
  agg = "count",
  hex_layer_prefix = "h3",
  point_layer_name = "points",
  min_zoom = 0L,
  max_zoom = 14L,
  base_zoom = NULL,
  h3_resolutions = NULL,
  source_crs = NULL,
  db_path = NULL,
  tile_format = "mvt",
  fade = FALSE,
  fade_overlap = 1L,
  overwrite = TRUE,
  quiet = FALSE
)
}
\arguments{
\item{input}{An \code{sf} data frame of POINT geometry, or a character SQL query
that returns a geometry column when executed against DuckDB. (DuckDB's
spatial functions such as \code{ST_Read()} and \code{read_parquet()} are available.)}

\item{output}{Character. Path for the output \code{.pmtiles} file.}

\item{agg}{Aggregation specification:
\itemize{
\item \code{"count"} (default): single \verb{count = COUNT(*)} property.
\item Named character vector of SQL aggregation expressions, e.g.
\code{c(n = "COUNT(*)", avg_pop = "AVG(pop)", total = "SUM(pop)")}.
\item Named list of \code{prop_name = c(fn, column)} for callers who don't want to
write SQL, e.g. \code{list(n = c("count", "*"), avg_pop = c("mean", "pop"))}.
Supported \code{fn} values: \code{"count"}, \code{"sum"}, \code{"mean"}/\code{"avg"}, \code{"min"},
\code{"max"}, \code{"median"}.
}}

\item{hex_layer_prefix}{Character. Prefix for the per-resolution hex MVT
layer names. Default \code{"h3"} produces \code{"h3_r01"}, \code{"h3_r02"}, etc.}

\item{point_layer_name}{Character. MVT layer name for raw points (default
\code{"points"}).}

\item{min_zoom, max_zoom}{Integer. Global zoom range (default 0--14).}

\item{base_zoom}{Integer or NULL. Zoom level at and above which raw points
take over from hex aggregations. Default \code{NULL} resolves to
\code{max_zoom - 2L}, clamped so \verb{min_zoom <= base_zoom <= max_zoom}. If
\code{base_zoom == min_zoom}, no hex layers are created.}

\item{h3_resolutions}{Optional override of the zoom -> H3 resolution
mapping. Accepts \code{NULL} (use built-in defaults), an unnamed integer
vector with \code{length(min_zoom:(base_zoom - 1L))} entries mapped
positionally, or a named integer vector with names that parse to integer
zoom levels (sparse overrides; defaults fill the rest). All resolutions
must be integers in \code{0:15}. The same resolution appearing in
non-contiguous zoom runs (e.g. zooms 4--5 and 8) is rejected.}

\item{source_crs}{Character or NULL. CRS of geometry returned by SQL
input, e.g. \code{"EPSG:4326"} (default for \code{sf} input, since \code{sf} is
auto-transformed to WGS84) or \code{"EPSG:3857"}. Ignored for \code{sf} input.
For SQL input, if \code{NULL}, a warning is emitted and the geometry is
assumed to be \code{"EPSG:4326"}.}

\item{db_path}{Character or NULL. Path to an existing DuckDB database file,
or \code{NULL}/\code{""} (default) for an in-memory database.}

\item{tile_format}{Character. \code{"mvt"} (default) or \code{"mlt"}.}

\item{fade}{Logical. If \code{TRUE}, adjacent hex layer zoom windows overlap by
\code{fade_overlap} zooms so \code{\link[=view_h3_tiles]{view_h3_tiles()}} can cross-fade between
resolutions. Default \code{FALSE} (disjoint windows, clean zoom breaks).}

\item{fade_overlap}{Integer. Zooms of overlap on each side (only used when
\code{fade = TRUE}, default \code{1L}).}

\item{overwrite}{Logical. Whether to overwrite an existing output file
(default \code{TRUE}).}

\item{quiet}{Logical. Suppress progress messages (default \code{FALSE}).}
}
\value{
The output file path (invisibly).
}
\description{
Aggregates points into H3 hexagons at zoom-appropriate resolutions and writes
a PMTiles archive in which low zooms show coarse hexagons, intermediate zooms
show progressively finer hexagons, and zooms at or above \code{base_zoom} show
individual points. Aggregations (count, sum, mean, etc.) are computed in
DuckDB via the H3 community extension; the function then assembles the
per-resolution hex layers and the raw-point layer via \code{\link[=freestile]{freestile()}}.
}
\details{
Each distinct H3 resolution becomes its own MVT source-layer (named
\code{"<hex_layer_prefix>_r<resolution>"}, e.g. \code{"h3_r05"}); raw points are
emitted as a separate source-layer (\code{point_layer_name}). With the default
\code{fade = FALSE}, per-layer zoom windows are disjoint so the rendered map
swaps between resolutions cleanly. With \code{fade = TRUE}, adjacent windows
overlap by \code{fade_overlap} zooms so the companion \code{\link[=view_h3_tiles]{view_h3_tiles()}} helper
can cross-fade between resolutions.

DuckDB and the H3 community extension are required. With \code{sf} input the
data is written to DuckDB via a temporary Parquet (or \code{dbWriteTable})
roundtrip; with character SQL input, the user query is wrapped in a
temporary view inside DuckDB.
}
\section{Point volume at \code{base_zoom}}{

The raw-point layer is encoded without thinning: every input point is
written to every tile that contains it from \code{base_zoom} up (and from
\code{base_zoom - fade_overlap} when \code{fade = TRUE}). For very large inputs the
tiles at the first point zoom can get heavy. Raising \code{base_zoom} defers
points to higher zooms, where each tile covers less area and so holds
fewer points. Per-layer feature dropping for the points layer is on the
roadmap.
}

\section{Antimeridian and polar cells}{

Hexagons that cross the antimeridian are split at +/-180 degrees so they
render correctly on both sides of the dateline instead of as
world-spanning slivers. The rare cells that contain a pole receive the
same split and render approximately (the polygon edge follows the cell
boundary vertices, so the polar cap itself is not filled).
}

\examples{
\dontrun{
library(sf)
pts <- st_as_sf(data.frame(
  x = runif(50000, -100, -80),
  y = runif(50000, 30, 45),
  w = rnorm(50000, 100, 10)
), coords = c("x", "y"), crs = 4326)

freestile_h3(pts, "wind.pmtiles",
  agg = c(n = "COUNT(*)", avg_w = "AVG(w)"),
  min_zoom = 2, max_zoom = 12, base_zoom = 10)

view_h3_tiles("wind.pmtiles", agg_column = "n")

# Cross-fade between resolutions
freestile_h3(pts, "wind_fade.pmtiles",
  agg = "count",
  min_zoom = 2, max_zoom = 12, base_zoom = 10,
  fade = TRUE)
}

}
\seealso{
\code{\link[=freestile]{freestile()}}, \code{\link[=view_h3_tiles]{view_h3_tiles()}}
}
