% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/freestile_h3.R
\name{view_h3_tiles}
\alias{view_h3_tiles}
\title{View an H3 hexagonal-binning PMTiles archive}
\usage{
view_h3_tiles(
  input,
  agg_column = NULL,
  stops = NULL,
  palette = "viridis",
  hex_opacity = 0.9,
  point_color = "#0868ac",
  point_radius = NULL,
  background_style = NULL,
  port = 8080
)
}
\arguments{
\item{input}{Path to a local \code{.pmtiles} file produced by \code{\link[=freestile_h3]{freestile_h3()}}.}

\item{agg_column}{Character or NULL. Aggregation column to drive the hex
color scale. If \code{NULL}, the first non-\code{h3_id} numeric field in the
metadata is used.}

\item{stops}{List with \code{values} and \code{colors} (equal length, sorted by
\code{values}) defining the shared color scale across all hex layers. If
\code{NULL}, the documented quick-look default is used.}

\item{palette}{Character. Named palette used to generate default \code{colors}
when \code{stops} is \code{NULL}. Currently supports \code{"viridis"} (default),
\code{"magma"}, \code{"plasma"}, \code{"cividis"}, \code{"inferno"}, \code{"rocket"}, \code{"mako"},
\code{"turbo"} if \code{viridisLite} is installed; otherwise falls back to a
five-color blue ramp.}

\item{hex_opacity}{Numeric. Peak fill opacity for hex layers (default 0.9).
In fade mode this is the opacity at each layer's center zoom.}

\item{point_color}{Character. Color for the raw-point circles
(default \code{"#0868ac"}).}

\item{point_radius}{mapgl expression or NULL. If \code{NULL}, a zoom-interpolated
default is used.}

\item{background_style}{mapgl style passed to \code{\link[mapgl:maplibre]{mapgl::maplibre()}}; if
\code{NULL} uses \code{mapgl::maplibre()} defaults.}

\item{port}{Integer. Port for the local PMTiles server (default 8080).}
}
\value{
A \code{mapgl} map object.
}
\description{
Reads the metadata from a PMTiles archive produced by \code{\link[=freestile_h3]{freestile_h3()}} and
builds a \code{mapgl} map with one fill layer per H3 resolution plus a circle
layer for raw points. Automatically detects whether the archive was built
with \code{fade = FALSE} (disjoint zoom windows, clean breaks) or \code{fade = TRUE}
(overlapping windows, cross-fade) and styles accordingly.
}
\details{
The default color scale is a documented quick-look ramp (5 evenly spaced
breaks across \verb{1, 10, 100, 1000, 10000}). For production maps, pass an
explicit \code{stops = list(values = ..., colors = ...)} derived from your data.
Embedding real aggregation statistics in PMTiles metadata is on the
roadmap.
}
\examples{
\dontrun{
view_h3_tiles("wind.pmtiles", agg_column = "n",
  stops = list(values = c(1, 10, 100, 1000, 10000),
               colors = viridisLite::viridis(5)))
}

}
\seealso{
\code{\link[=freestile_h3]{freestile_h3()}}, \code{\link[=view_tiles]{view_tiles()}}
}
